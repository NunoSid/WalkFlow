{
  "name": "walkflow",
  "version": "1.0.0",
  "private": false,
  "description": "WalkFlow monorepo",
  "author": "Nuno da Silva Magalhaes",
  "license": "MIT",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "prisma:setup": "cd server && npx prisma migrate dev --name init && npx ts-node src/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
